# DUNGEON CRAFT — lancement WSL

Depuis WSL :

```bash
cd /mnt/c/Users/mirco/Downloads/game-in-go-DUNGEON-CRAFT/game-in-go-
go run ./graphique
```

Ou :

```bash
bash run_graphique.sh
```

Si Go demande de télécharger des dépendances, laisse la commande terminer puis relance `go run ./graphique`.
